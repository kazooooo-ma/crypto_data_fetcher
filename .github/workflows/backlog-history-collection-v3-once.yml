name: Backlog History Collection V3 One-Off

on:
  push:
    paths:
      - '.github/workflows/backlog-history-collection-v3-once.yml'

permissions:
  contents: read

jobs:
  collect:
    name: collect-${{ matrix.shard }}
    runs-on: ubuntu-latest
    timeout-minutes: 120
    strategy:
      fail-fast: false
      matrix:
        shard: [0, 1, 2, 3, 4, 5]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python -m pip install requests pymupdf beautifulsoup4
      - name: Collect and parse quarterly history
        run: >-
          python scripts/backlog_history_collection_v3.py collect
          --shard ${{ matrix.shard }} --shards 6
          --out out/shard-${{ matrix.shard }} --workers 10
      - uses: actions/upload-artifact@v4
        with:
          name: backlog-history-v3-part-${{ matrix.shard }}
          path: out/shard-${{ matrix.shard }}/
          if-no-files-found: error
          retention-days: 1

  aggregate:
    name: aggregate-and-reconcile
    needs: collect
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python -m pip install requests pymupdf beautifulsoup4
      - uses: actions/download-artifact@v4
        with:
          pattern: backlog-history-v3-part-*
          path: parts
          merge-multiple: false
      - name: Aggregate and run current-signal reconciliation
        run: >-
          python scripts/backlog_history_collection_v3.py aggregate
          --parts parts --out out/aggregate
      - uses: actions/upload-artifact@v4
        with:
          name: backlog-history-collection-v3
          path: out/aggregate/
          if-no-files-found: error
          retention-days: 1
