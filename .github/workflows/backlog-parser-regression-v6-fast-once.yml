name: Backlog Parser V6 Fast Regression One-Off

on:
  push:
    paths:
      - '.github/workflows/backlog-parser-regression-v6-fast-once.yml'

permissions:
  contents: read

jobs:
  regression:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python -m pip install requests pymupdf beautifulsoup4
      - run: python scripts/backlog_parser_regression_v6_fast.py
      - uses: actions/upload-artifact@v4
        with:
          name: backlog-parser-regression-v6-fast
          path: out/backlog-parser-regression-v6-fast/
          if-no-files-found: error
          retention-days: 1
