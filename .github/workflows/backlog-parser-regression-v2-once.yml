name: Backlog Structured Parser V2 Regression One-Off

on:
  push:
    paths:
      - '.github/workflows/backlog-parser-regression-v2-once.yml'

permissions:
  contents: read

jobs:
  regression:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python -m pip install requests pymupdf beautifulsoup4
      - name: Run parser v2 regression
        run: python scripts/backlog_parser_regression_v2.py
      - uses: actions/upload-artifact@v4
        with:
          name: backlog-parser-regression-v2
          path: out/backlog-parser-regression-v2/
          if-no-files-found: error
          retention-days: 1
